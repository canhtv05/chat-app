import Modal from '~/components/Modals/Modal';

function ModalAddGroup({ isOpen, setIsOpen }) {
    return (
        <Modal isOpen={isOpen} setIsOpen={setIsOpen} title="Tạo nhóm" outline>
            <div>ok</div>
        </Modal>
    );
}

export default ModalAddGroup;
