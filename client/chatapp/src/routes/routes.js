import HomePage from '~/pages/Home/HomPage';

const publicRoutes = [
    {
        path: '/',
        component: HomePage,
    },
];

const privateRoutes = [];

export { publicRoutes, privateRoutes };
